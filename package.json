{
  "name": "bugpilot-js-libraries",
  "private": "true",
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-plugin-prettier": "^5.1.2",
    "eslint-plugin-react": "^7.33.2",
    "lerna": "^8.0.1",
    "prettier": "^3.1.1"
  },
  "version": "0.0.0",
  "scripts": {
    "build": "lerna run build",
    "release": "lerna run release",
    "clean": "lerna run clean && rm -rf dist && rm -rf node_modules && pnpm store prune"
  }
}
